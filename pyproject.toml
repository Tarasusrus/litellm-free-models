[project]
name = "litellm-free-models"
version = "0.3.0"
description = "LiteLLM proxy that aggregates free LLM APIs from multiple providers, with load balancing, cooldown, and fallback chains."
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "sb" },
]
readme = "README.md"

[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = [
    ".cache",
    "k8s/",
    "multi-instance/k8s/",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP"]
# W503 (flake8) doesn't exist in ruff and used to make ruff fail at
# config parsing – don't re-add it.
ignore = [
    "E501",
    "E402",
]

# Fork: upstream code trips UP038 (isinstance with a tuple) on the ruff
# versions CI installs; ignored here instead of rewriting upstream lines.
[tool.ruff.lint.per-file-ignores]
"find-shared-models.py" = ["UP038"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
