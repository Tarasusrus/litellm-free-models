"""Fork-only code (see docs/adr/0001-fork-conventions.md).

Everything this fork adds on top of natorus87/litellm-free-models lives
here, so a sync with upstream never has to merge inside upstream's files.
"""
